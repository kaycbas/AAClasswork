<button 
  class="follow-toggle" 
  data-id="<%= user.id %>" 
  data-initial_follow_state="<%= current_user.follows?(user) ? "followed" : "unfollowed" %>">
  <%= current_user.follows?(user) ? "unfollow" : "follow" %>
</button>
